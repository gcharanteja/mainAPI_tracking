model:
  name: ResNet50
  pretrained: true
  num_classes: 10

training:
  learning_rate: 0.001
  batch_size: 32
  epochs: 10
  optimizer: Adam
  weight_decay: 0.0001

data:
  dataset: CIFAR-10
  augmentation:
    horizontal_flip: true
    random_crop: true
    normalize: true
